{
  "name": "react-nodejs-todo-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "run-p dev:frontend dev:backend",
    "heroku-postbuild": "npm install --include=dev --prefix frontend && npm run build --prefix frontend && npm install --include=dev --prefix backend && npm run build --prefix backend",
    "start": "node backend/dist/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
